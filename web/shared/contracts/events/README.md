# Event Schemas

Event-driven workflows are not implemented yet, but this directory is reserved
for JSON/YAML schemas that describe messages published to asynchronous systems
(e.g., Pub/Sub, SQS). Keep the schemas versioned (`event_name/v1.yaml`) so
consumers can upgrade independently.
